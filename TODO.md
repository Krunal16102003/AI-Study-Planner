- [x] Implement responsive admin dashboard shell layout fixes in `frontend/src/styles.css`
  - [x] Introduce sidebar-width CSS variables for expanded (280px) and collapsed (90px)
  - [x] Update `.premium-sidebar` + `.dashboard-topbar` + `.dashboard-main` so main content offsets correctly
  - [x] Prevent overlap/cut-off by tightening overflow rules and z-index
  - [x] Ensure smooth transitions (300ms) on sidebar collapse/expand
  - [ ] Fix top spacing/padding so topbar and content align
  - [ ] Restore consistent grid/card alignment with `min-width: 0` and no horizontal scrolling
  - [ ] Validate responsive breakpoints (desktop/tablet/mobile)
- [x] Run frontend build/dev checks (no horizontal scrolling, charts resize, cards fully visible)



